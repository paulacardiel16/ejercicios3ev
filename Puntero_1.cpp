#include <stdio.h> //libreria basica de C
#include <iostream>

using namespace std;
int main() {
    int x; x=3;
    int y; y=5;
    char salida;
    printf("%d",x); //Significa: escribe la x siendo esta un entero; si fuera float se pone %f y si es caracter %c
    printf("%d",y);
    
    cin >> salida;
    }
